#include "container.h"
#include "wrap.h"

int main(int argc, char* argv[])
{
    wrap(new Container);
    //std::shared_ptr<Container> container(new Container);
    //static_cast<void>(containercontainer); //Not used
}
